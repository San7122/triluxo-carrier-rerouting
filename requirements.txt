# Core agent framework
langgraph>=1.0,<2.0
langchain-core>=1.0,<2.0

# Closed-source model client (Claude)
anthropic>=0.40
langchain-anthropic>=1.0

# Open-source model client (Llama/Qwen via Groq's OpenAI-compatible API)
openai>=1.40

# Config + deck generation
python-dotenv>=1.0
python-pptx>=0.6.23

# Tests
pytest>=8.0
